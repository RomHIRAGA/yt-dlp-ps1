<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="YouTube Downloader GUI v2.0"
    Height="420" Width="650"
    WindowStartupLocation="CenterScreen"
    ResizeMode="NoResize"
    Background="#1F1F1F">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- URL入力 -->
        <TextBlock Text="YouTube URL" Foreground="White" FontSize="14"/>
        <TextBox Name="UrlBox" Grid.Row="1" Background="#333" Foreground="White"
                 Height="30" FontSize="14"/>

        <!-- 解像度 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="解像度：" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
            <ComboBox Name="ResolutionBox" Width="120" Margin="10,0,0,0"
                      SelectedIndex="0" Background="#333" Foreground="White">
                <ComboBoxItem Content="1080p（推奨）" Tag="301"/>
                <ComboBoxItem Content="720p" Tag="300"/>
                <ComboBoxItem Content="480p" Tag="94"/>
                <ComboBoxItem Content="360p" Tag="18"/>
            </ComboBox>
            <Button Name="FolderButton" Content="保存フォルダ選択" Margin="20,0,0,0"
                    Width="150" Background="#444" Foreground="White"/>
        </StackPanel>

        <!-- ログ -->
        <TextBox Name="LogBox" Grid.Row="3" Background="#111" Foreground="#E0E0E0"
                 FontSize="12" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 IsReadOnly="True"/>

        <!-- ボタン + 進捗バー -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Name="DownloadButton" Content="Download" Width="140" Height="35"
                    Background="#007ACC" Foreground="White" FontWeight="Bold"/>

            <ProgressBar Name="ProgressBar" Width="300" Height="20" Margin="20,0,0,0"
                         Minimum="0" Maximum="100" Value="0"/>
        </StackPanel>
    </Grid>
</Window>
